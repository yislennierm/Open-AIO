{
  "name": "@open-aio/core",
  "version": "0.1.0",
  "private": true,
  "description": "Native Open AIO core for RawUSB display streaming.",
  "main": "index.js",
  "scripts": {
    "build": "napi build --platform --release",
    "build:service": "cargo build --release --bin open-aio-service",
    "check": "cargo check --all-targets",
    "test:packet": "node test/protocol-smoke.js"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}
